export function Gallery(props) {
  props.elements;
  return (
    <ul>
      <li>{props.elements}</li>
      <li>{props.elements}</li>
      <li>{props.elements}</li>
      <li>{props.elements}</li>
    </ul>
  );
}
